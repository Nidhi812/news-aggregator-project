server:
  port: 8085

spring:
  application:
    name: activity-service

  datasource:
    url: jdbc:mysql://localhost:3306/news_aggregator
    username: ${DB_USERNAME:root}
    password: "${DB_PASSWORD:Mihu@08}"
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

  cloud:
    discovery:
      enabled: true

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka

  instance:
    prefer-ip-address: true
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html



# server:
#   port: 8085

# spring:
#   application:
#     name: activity-service

#   datasource:
#     url: jdbc:mysql://localhost:3306/news_aggregator
#     username: root
#     password: root
#     driver-class-name: com.mysql.cj.jdbc.Driver

#   jpa:
#     hibernate:
#       ddl-auto: update
#     show-sql: true
#     database-platform: org.hibernate.dialect.MySQL8Dialect

# springdoc:
#   api-docs:
#     path: /v3/api-docs
#   swagger-ui:
#     path: /swagger-ui.html